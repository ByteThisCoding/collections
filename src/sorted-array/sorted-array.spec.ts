import { runSortedListTests } from "../tests-common/sorted-list";
import { SortedArray } from "./sorted-array";

describe("SortedArray", () => {
    runSortedListTests("SortedArray", SortedArray);
});
